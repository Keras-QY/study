<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--<properties resource="jdbc.properties"/>-->
    <typeAliases>
        <package name="com.kadima.Mybatis"/>
    </typeAliases>
    <!--<environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${read_driver}"/>
                <property name="url" value="${read_url}"/>
                <property name="username" value="${read_username}"/>
                <property name="password" value="${read_password}"/>
            </dataSource>
        </environment>
    </environments>-->

    <!--插件-->
    <plugins>
        <!--分页插件-->
        <!--<plugin interceptor="com.bjpowernode.plugin.pager.PagerPlugin">
            <property name="isEnable" value="true"/>
        </plugin>-->
        <plugin interceptor="com.kadima.Mybatis.plugin.MyPlugin"/>
    </plugins>

    <mappers>
        <mapper resource="com/kadima/Mybatis/mapper/AccountMapper.xml"/>
    </mappers>

</configuration>
